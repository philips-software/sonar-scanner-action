{
  "name": "sonar-scanner",
  "version": "1.1.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "ncc build index.ts -o dist",
    "test": "jest --no-cache",
    "test:watch": "jest --watch --watchAll"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@types/jest": "^28.1.7",
    "@types/node": "^16.18.4",
    "@vercel/ncc": "^0.34.0",
    "jest": "^28.1.3",
    "jest-cli": "^29.2.0",
    "ts-jest": "^28.0.7",
    "typescript": "^4.8.4"
  }
}
