{
  "name": "sonar-scanner",
  "version": "1.1.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "ncc build index.ts -o dist",
    "test": "jest --no-cache",
    "test:watch": "jest --watch --watchAll"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/jest": "^28.1.7",
    "@types/node": "^20.10.0",
    "@vercel/ncc": "^0.38.1",
    "jest": "^28.1.3",
    "jest-cli": "^29.7.0",
    "ts-jest": "^28.0.7",
    "typescript": "^5.2.2"
  }
}
