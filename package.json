{
  "name": "sonar-scanner",
  "version": "1.1.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "ncc build index.ts -o dist",
    "test": "jest --no-cache",
    "test:watch": "jest --watch --watchAll"
  },
  "dependencies": {
    "@actions/core": "^1.9.0",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^5.0.3"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "@types/node": "^16.11.45",
    "@vercel/ncc": "^0.34.0",
    "jest": "^28.1.3",
    "jest-cli": "^28.1.2",
    "ts-jest": "^28.0.7",
    "typescript": "^4.7.2"
  }
}
